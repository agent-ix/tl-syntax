[package]
name = "tl-release-gate"
version = "0.1.0"
edition = "2021"
rust-version = "1.98.1"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8.2"

[lints.rust]
unsafe_code = "forbid"
